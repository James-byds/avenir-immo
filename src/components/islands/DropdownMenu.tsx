// Îlot React «DropdownMenu» — hydraté via client:visible. Dernier recours — préférer <select> natif enhancé par ds-script.js.
// Fait partie du budget JS du site (4 îlots max). À implémenter.
export default function DropdownMenu() {
  return null;
}
