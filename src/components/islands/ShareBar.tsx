// Îlot React «ShareBar» — hydraté via client:visible. Barre de partage d'article.
// Fait partie du budget JS du site (4 îlots max). À implémenter.
export default function ShareBar() {
  return null;
}
