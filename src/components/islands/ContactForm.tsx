// Îlot React «ContactForm» — hydraté via client:visible. Formulaire de contact avec validation.
// Fait partie du budget JS du site (4 îlots max). À implémenter.
export default function ContactForm() {
  return null;
}
